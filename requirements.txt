# Core dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0

# Data processing
pandas>=2.2.0
numpy>=2.0.0

# Stock data API
yfinance>=0.2.40

# Database
# SQLite is built into Python, no additional package needed

# Optional but recommended
python-multipart>=0.0.9
httpx>=0.26.0

# Vercel deployment (ASGI adapter)
mangum>=0.19.0
